# Copy to .env and fill in. The app runs without a key in offline
# (rule-based) mode, so this is only needed for real LLM agent output.
GEMINI_API_KEY=
LLM_PROVIDER=gemini
LLM_MODEL=gemini-2.0-flash
